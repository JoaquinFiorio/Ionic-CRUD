import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joaquin.app',
  appName: 'ionic-angular-basic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
